name: LEAPs Bot Daily Check

on:
  schedule:
    - cron: '33 10 * * 1-5'
      timezone: America/New_York
  workflow_dispatch:

concurrency:
  group: leaps-bot-run
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  daily-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -e .

      - name: Run daily check
        id: bot_run
        env:
          ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
          ALPACA_SECRET_KEY: ${{ secrets.ALPACA_SECRET_KEY }}
        run: leaps-bot run --config config.yaml --state data/state.json
        continue-on-error: true

      - name: Commit updated state
        if: always()
        run: |
          git config user.name "leaps-bot"
          git config user.email "leaps-bot@github-actions"
          git add data/state.json
          if ! git diff --cached --quiet; then
            git commit -m "Update bot state $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            git pull --rebase origin main || true
            PUSH_OK=0
            for i in 1 2 3; do
              if git push origin main; then
                PUSH_OK=1
                break
              fi
              git pull --rebase origin main || true
            done
            if [ "$PUSH_OK" -ne 1 ]; then
              echo "::error::Failed to push state after 3 attempts."
              exit 1
            fi
          else
            echo "No state changes to commit"
          fi

      - name: Fail if bot run failed
        if: steps.bot_run.outcome == 'failure'
        run: |
          echo "Bot run failed — check logs above. State was committed if changed."
          exit 1
